# This file is intentionally minimal.
# The active railway.toml is in backend/railway.toml
# Railway root directory for this service must be set to: backend
#
# To set root directory in Railway:
#   Service Settings -> Source -> Root Directory -> "backend"
